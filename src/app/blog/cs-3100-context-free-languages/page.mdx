import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
  author: 'Sathya Tadinada',
  date: '2026-04-21',
  title: 'CS 3100: Context-Free Languages and Pushdown Automata',
  description:
    'A sample follow-up post showing how the second entry in a series is linked from the article layout.',
  readingMinutes: 5,
  series: {
    slug: 'cs-3100',
    part: 2,
  },
  tags: ['context-free languages', 'cs 3100', 'sipser'],
  // archived: true,
}

export default function Page({ children }) {
  return <ArticleLayout article={article}>{children}</ArticleLayout>
}

This is placeholder content for testing the second entry in a series.

## Architecture As A Follow-Up

After the product decisions are established, a second post can move into the
system shape: services, data flow, jobs, plugin boundaries, and deployment
details.

## What This Post Would Cover

- The app and API boundary.
- How plugins communicate with the backend.
- Why deployment ergonomics mattered for a self-hosted project.

The series box above should mark this as part two and link back to part one.
